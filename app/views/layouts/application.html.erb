<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "fml | Secret Inlet5715" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "fml | Secret Inlet5715" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <!-- <%= render 'layouts/navigation' %> -->
      <div class="navbar navbar-inverse">
        <div class="navbar-inner">
          <a class="navbar-brand" href="#">FML</a>
          <%= link_to 'Code', 'https://github.com/JaimeLynSchatz/r-14-fml-app.git', :class => 'navbar-brand', :target => '_blank' %>
    </header>
    <main role="main">
       <!-- <%= render 'layouts/messages' %> -->
       <%= yield %>
    </main>
    
  </body>
</html>
